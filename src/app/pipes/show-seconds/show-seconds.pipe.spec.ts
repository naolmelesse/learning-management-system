import { ShowSecondsPipe } from './show-seconds.pipe';

describe('ShowSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
